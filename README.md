# Notes-App
A simple command line notes app built in C++.

## To build:
```
$ git clone
$ cd ${cloned-directory}
$ mkdir build
$ cd build/
$ cmake ../
$ make
$ ./Notes
###-optional-###
$ ln -s ./Notes ../
```
